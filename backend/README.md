# Backend of Kejaprime application
# Installation
- cd /backend
- pip install -r requirements.txt

- create a .env file, refer to env_example for the format

# run the backend server
- python main.py / python3 main.py
